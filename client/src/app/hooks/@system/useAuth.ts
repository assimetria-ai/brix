// @system — re-exports useAuth from the main auth store.
// The full AuthProvider (login/register/logout/refresh) lives in store/@system/auth.tsx.
export { useAuth } from '../../store/@system/auth'
